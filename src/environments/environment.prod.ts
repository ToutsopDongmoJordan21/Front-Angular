export const environment = {
  production: true,
  baseUrl: 'http://localhost:8080/api'
  //baseUrl: 'https://caref-back.herokuapp.com/api'

   /*  "name": "caref-front",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "http-server-spa dist/caref-front index.html $PORT",
    "build": "ng build",
    "test": "ng test",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "heroku-postbuild": "ng build --prod && npm install -g http-server-spa"
  }, */
};
